// Export all processors
const SnapProcessor = require('./snapProcessor');
const GridsetProcessor = require('./gridsetProcessor');
const TouchChatProcessor = require('./touchChatProcessor');

module.exports = {
    SnapProcessor,
    GridsetProcessor,
    TouchChatProcessor
};
